@model IList<ProductOverviewModel>
@using Nop.Web.Models.Catalog;
@if (Model.Count > 0)
{
    @*<ul class="col-xs-12 son-bakilanlar">
        <li class="col-xs-12 son-bakilan">
            <h4>@T("Products.RecentlyViewedProducts")</h4>
        </li>
        @for (int i = 0; i < Model.Count; i++)
                {
                    var product = Model[i];
            <li class="col-xs-12 son-bakilan">
                @if (!String.IsNullOrEmpty(product.DefaultPictureModel.ImageUrl))
                {
                    <a href="@Url.RouteUrl("Product", new { SeName = product.SeName })" title="@product.DefaultPictureModel.Title">
                        <img alt="@product.DefaultPictureModel.AlternateText" class="img-responsive col-xs-5" src="@product.DefaultPictureModel.ImageUrl" title="@product.DefaultPictureModel.Title" />
                    </a>
                }
                <a href="@Url.RouteUrl("Product", new { SeName = product.SeName })"><span class="son-bakilan__urun-adi col-xs-7">@product.Name</span></a>
            </li>
        }
    </ul>*@
    @*
    <div class="block block-recently-viewed-products">
        <div class="title">
            <strong>@T("Products.RecentlyViewedProducts")</strong>
        </div>
        <div class="listbox">
            <ul class="list">
                @for (int i = 0; i < Model.Count; i++)
                {
                    var product = Model[i];
                    <li @(i == Model.Count - 1 ? @Html.Raw(" class=\"last\"") : null)>
                        @if (!String.IsNullOrEmpty(product.DefaultPictureModel.ImageUrl))
                        {
                            <a class="product-picture" href="@Url.RouteUrl("Product", new { SeName = product.SeName })" title="@product.DefaultPictureModel.Title">
                                <img alt="@product.DefaultPictureModel.AlternateText" src="@product.DefaultPictureModel.ImageUrl" title="@product.DefaultPictureModel.Title" />
                            </a>
                        }
                        <a class="product-name" href="@Url.RouteUrl("Product", new { SeName = product.SeName })">@product.Name</a>
                    </li>
                }
            </ul>
        </div>
    </div>
        *@
}